<h1>PinPin</h1>

<% if user_signed_in? %>

	<p>You did it!</p>

	<%= link_to "Add a Pin", new_pin_path, class: "btn btn-primary" %>

<% else %>

	<%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary" %>
	<%= link_to "Login", new_user_session_path, class: "btn btn-default" %>

<% end %>

